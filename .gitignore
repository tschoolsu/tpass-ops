# 頂層 = tpass-ops repo。只追蹤 ops 層（scripts / deploy / docs / agent md）。
# 策略：預設全部忽略，再白名單 ops 檔。這樣任何新加入的服務子 repo
# （各自有 .git）或雜物都「自動安全」，不可能被誤 commit。
# 服務註冊表 services.json 已搬到並排的 tpass-registry/ repo（public），
# 跟其他服務 repo 一樣被 /* 排除——這裡不再有它的白名單。
# 頂層 md 只留 agent 必要的（AGENTS.md + 指標 CLAUDE.md）；其餘文檔一律在 docs/。

# --- 預設忽略一切 ---
/*

# --- 白名單：ops 層才追蹤 ---
!/.gitignore
!/.github/
!/AGENTS.md
!/CLAUDE.md
!/scripts/
!/deploy/
!/docs/

# --- deploy/ 內的機密不追蹤（其餘 deploy/ 檔照追蹤）---
# 注意：下面 `.env.*` 那條保險絲比對的是「以 .env. 開頭」的檔名，配不到 backup.env，必須明列。
/deploy/host.env
/deploy/backup.env

# --- 誤建目錄，含機密；保留不刪但永不進 git（已被上面 /* 忽略，明列一次求明確）---
/~/

# --- 保險絲：任何 .env / 憑證 / 私鑰即使誤放別處也擋下 ---
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
id_rsa*
id_ed25519*
*.ppk
.DS_Store
*.log
