# 部署主機連線設定範本。
# cp 成同目錄的 host.env（已被 .gitignore 排除，永不進 git）再填真值。
# 主機 IP / 帳號是機密，只存在 host.env，不得寫進任何被追蹤的檔案。
# 連線一律走 scripts/ssh.sh（它會讀這裡的值）。
DEPLOY_HOST=your.server.host
DEPLOY_USER=youruser

# 選填：自架 Uptime Kuma 的位址與唯讀 API key（Kuma 後台 → Settings → API Keys）。
# 填了 `tpass status` 才會多印「== 監控 ==」段（各服務 up/down + 註冊表比對出
# 「deployed 卻沒有監控」的服務）。兩個都留空的話整段安靜跳過。
# Kuma 跑在部員的機器上，不是主機——見 monitoring/HANDOFF.md。
KUMA_BASE_URL=
KUMA_API_KEY=
